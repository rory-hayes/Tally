import type { ClientRow } from "@/lib/repositories/clients";

export const clientFixture: ClientRow = {
  id: "client-123",
  name: "Acme Corp",
  country: "IE",
  payroll_system: "Sage",
  employees_processed: 0,
};


